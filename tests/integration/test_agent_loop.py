"""Integration tests for the agent orchestrator loop."""

from __future__ import annotations

import pytest


class TestAgentLoop:
    @pytest.mark.asyncio
    async def test_agent_loop_placeholder(self) -> None:
        """Placeholder — requires mocked LLM client."""
        assert True
